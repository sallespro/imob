{
  "name": "imob",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "concurrently --kill-others-on-fail -n api,vite -c cyan,green \"node scrape-server.mjs --seed\" \"npm run dev --prefix imob-app\"",
    "seed": "node seed.mjs",
    "seed:force": "node seed.mjs --force",
    "geocode": "node geocode.mjs",
    "geocode:all": "node geocode.mjs --all",
    "test": "playwright test",
    "scrape": "node scripts/scraper.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "sharp": "^0.35.2"
  }
}
